# 停电用户时间趋势实际停电次数接口文档

## 1. 接口概述

该接口用于分页查询时间趋势用户，并为当前页每位用户补充指定时间范围内的实际停电记录总数。

- 请求方法：`POST`
- 请求路径：`/api/v1/outage-users/time-trend/users/real-count`
- 请求类型：`application/json`
- 成功响应类型：JSON 数组

前端只需要调用本系统接口，不需要传递数据中台 appcode 或 ESB Token。上游鉴权由后端处理。

## 2. 请求头

| 请求头 | 必填 | 值 |
| --- | --- | --- |
| `Content-Type` | 是 | `application/json` |

## 3. 请求参数

请求参数位于 JSON 请求体中。

| 参数 | 类型 | 必填 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| `beginTime` | string | 是 | `20260701` | 用户列表查询开始日期，格式为 `YYYYMMDD`。该值原样传给用户列表上游接口。 |
| `endTime` | string | 是 | `20260701` | 用户列表查询结束日期，格式为 `YYYYMMDD`，并用于计算实际停电记录的查询时间范围。 |
| `intervalType` | string | 是 | `two` | 时间范围类型，只允许 `one` 或 `two`。`one` 表示近期一个自然月，`two` 表示近期两个自然月。 |
| `pageNum` | number/string | 是 | `1` | 用户列表页码，必须是正整数；同时作为停电详情查询的 `page`。 |
| `pageSize` | number/string | 是 | `10` | 每页用户数量，必须是正整数；同时作为停电详情查询的 `perPage`。 |
| `returnTotalNum` | string/boolean | 否 | `"true"` | 是否要求用户列表上游返回记录总数，参数原样转发。推荐按上游格式传字符串 `"true"`。 |
| `cityId` | string | 否 | `16401` | 地市 ID，参数原样转发。 |
| `outageLevel` | string | 否 | `3` | 停电次数筛选条件，参数原样转发。例如 `3` 表示筛选停电次数 3 次及以上的用户。 |

### 3.1 请求示例

```json
{
  "beginTime": "20260701",
  "endTime": "20260701",
  "pageNum": 1,
  "pageSize": 10,
  "returnTotalNum": "true",
  "cityId": "16401",
  "intervalType": "two",
  "outageLevel": "3"
}
```

## 4. 时间范围计算规则

实际停电记录的查询时间只根据 `endTime` 和 `intervalType` 计算，起止时刻均为 `23:59:59`。

| `endTime` | `intervalType` | 实际查询 `beginTime` | 实际查询 `endTime` |
| --- | --- | --- | --- |
| `20260701` | `one` | `2026-06-01 23:59:59` | `2026-07-01 23:59:59` |
| `20260701` | `two` | `2026-05-01 23:59:59` | `2026-07-01 23:59:59` |

月份按自然月计算。目标月份不存在同一天时，会取该月最后一天。例如从 `2026-03-31` 向前一个月，开始日期为 `2026-02-28`。

## 5. 后端处理流程

1. 将前端请求参数原样传给时间趋势用户列表接口：

   ```text
   /cst/xnykgkc/rds/post_ads_cst_xnykgkc_detail_key_sen_list
   ```

2. 读取当前页用户列表中的 `consNo`。
3. 对当前页每位用户分别调用一次停电详情接口：

   ```text
   /realMeasCenter/dsOutageAnalysis/outageEvent/queryOutageUserDetail
   ```

4. 每次详情请求的 `consNos` 数组只包含当前一位用户，但当前页所有用户都会依次查询。
5. 读取详情响应中的 `total`，写入对应用户记录的 `total` 字段。

例如当前请求中 `pageNum=1`、`pageSize=10`、`endTime=20260701`、`intervalType=two`，某位用户编号为 `1601173658324`，后端发出的单次详情请求为：

```json
{
  "beginTime": "2026-05-01 23:59:59",
  "endTime": "2026-07-01 23:59:59",
  "consNos": ["1601173658324"],
  "page": 1,
  "perPage": 10
}
```

## 6. 成功响应

HTTP 状态码：`200`

成功响应直接返回当前页用户数组，不包含 `code`、`data`、`rows`、`totalNum` 等外层字段。即使请求传入 `returnTotalNum="true"`，聚合接口仍然返回数组本身。

### 6.1 响应字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `consName` | string/null | 用户名称。 |
| `consNo` | string/null | 用户编号。 |
| `countyName` | string/null | 所属区县供电单位名称。 |
| `intervalOne` | number/string/null | 近期一个月停电次数，由用户列表上游返回。 |
| `intervalTwo` | number/string/null | 近期两个月停电次数，由用户列表上游返回。 |
| `total` | number/string | 按本接口计算的时间范围查询到的实际停电记录总数。详情响应没有可用 `total` 或用户编号为空时返回 `0`。 |

### 6.2 响应示例

```json
[
  {
    "consName": "沙志武1",
    "consNo": "1601173658324",
    "countyName": "国网冀北迁安市供电公司",
    "intervalOne": 3,
    "intervalTwo": 6,
    "total": 4
  },
  {
    "consName": "张为",
    "consNo": "1601173658441",
    "countyName": "国网冀北迁安市供电公司",
    "intervalOne": 2,
    "intervalTwo": 5,
    "total": 2
  }
]
```

当前页没有用户时返回：

```json
[]
```

## 7. 参数校验失败

参数校验失败时，HTTP 状态码为 `400`。

### 7.1 常见校验错误

| 场景 | 错误信息 |
| --- | --- |
| 缺少必填字段 | `<字段名> is required` |
| `beginTime` 格式错误 | `beginTime must be formatted as YYYYMMDD` |
| `endTime` 格式错误 | `endTime must be formatted as YYYYMMDD` |
| `endTime` 不是有效日期 | `endTime must be a valid date formatted as YYYYMMDD` |
| `intervalType` 不是 `one` 或 `two` | `intervalType must be one or two` |
| `pageNum` 不是正整数 | `pageNum must be a positive integer` |
| `pageSize` 不是正整数 | `pageSize must be a positive integer` |

错误响应示例：

```json
{
  "code": 400,
  "message": "invalid request payload",
  "errors": [
    "intervalType must be one or two",
    "pageNum must be a positive integer"
  ]
}
```

请求体不是 JSON 对象时返回：

```json
{
  "code": 400,
  "message": "request body must be a JSON object"
}
```

## 8. 上游请求失败

用户列表或任意一位用户的停电详情查询失败时，本接口整体返回错误，不返回部分成功的数据。

常见错误响应示例：

```json
{
  "code": 502,
  "message": "outage time trend user list request failed",
  "detail": "upstream status: 500"
}
```

```json
{
  "code": 502,
  "message": "outage user detail request failed",
  "detail": "upstream status: 500"
}
```

网络异常、超时或鉴权异常时，`message` 和 HTTP 状态码可能由后端上游客户端根据实际异常返回，响应中可能包含 `detail` 字段。

## 9. 前端 TypeScript 类型

```typescript
export interface OutageTimeTrendRealCountRequest {
  beginTime: string;
  endTime: string;
  intervalType: "one" | "two";
  pageNum: number | string;
  pageSize: number | string;
  returnTotalNum?: "true" | "false" | boolean;
  cityId?: string;
  outageLevel?: string;
}

export interface OutageTimeTrendRealCountItem {
  consName: string | null;
  consNo: string | null;
  countyName: string | null;
  intervalOne: number | string | null;
  intervalTwo: number | string | null;
  total: number | string;
}
```

## 10. 前端调用示例

```typescript
const response = await fetch(
  "/api/v1/outage-users/time-trend/users/real-count",
  {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      beginTime: "20260701",
      endTime: "20260701",
      pageNum: 1,
      pageSize: 10,
      returnTotalNum: "true",
      cityId: "16401",
      intervalType: "two",
      outageLevel: "3",
    }),
  },
);

if (!response.ok) {
  const error = await response.json();
  throw new Error(error.message ?? "查询失败");
}

const users: OutageTimeTrendRealCountItem[] = await response.json();
```

## 11. 对接注意事项

- `intervalType` 必须传入，且只能是小写的 `one` 或 `two`。
- `beginTime` 和 `endTime` 使用 `YYYYMMDD`，不要传 `YYYY-MM-DD`。
- `pageNum` 和 `pageSize` 可以传正整数或正整数字符串，推荐前端统一传数字。
- 响应是数组，不要按 `response.data.rows` 读取；使用 Axios 时通常从 `response.data` 直接取得数组。
- `total` 是实际停电详情记录总数，不是当前用户列表的总人数。
- 当前页有 10 位有效用户时，后端会额外进行 10 次停电详情查询，因此响应时间会高于普通列表接口。
