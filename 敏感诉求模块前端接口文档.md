# 敏感诉求模块前端接口文档

本文档描述前端需要调用的本系统接口。前端只调用 `/api/v1/...` 接口，不需要也不应该直接调用数据中台接口。

## 通用说明

- Base URL：按部署环境配置，例如本地开发可为 `http://localhost:5000`
- 统一前缀：`/api/v1`
- 业务接口方法：`POST`
- 请求体：`application/json`
- 响应体：`application/json`
- 日期格式：`YYYYMMDD`，例如 `20260701`
- 分页字段：`pageNum`、`pageSize` 必须为正整数，可以传数字或数字字符串
- 认证：前端不需要传数据中台 `appcode`，后端会自动补充
- 响应说明：业务接口会把数据中台返回的 JSON 基本原样返回给前端

## 通用成功响应

不同接口的 `data` 结构不同，但外层通常为：

```json
{
  "data": [],
  "errCode": 0,
  "requestId": "string",
  "errMsg": "success",
  "apiLog": null
}
```

分页列表接口的 `data` 通常为：

```json
{
  "data": {
    "totalNum": 100,
    "pageSize": 10,
    "rows": [],
    "pageNum": 1
  },
  "errCode": 0,
  "requestId": "string",
  "errMsg": "success",
  "apiLog": null
}
```

## 通用错误响应

请求体不是 JSON 对象：

```json
{
  "code": 400,
  "message": "request body must be a JSON object"
}
```

参数校验失败：

```json
{
  "code": 400,
  "message": "invalid request payload",
  "errors": [
    "beginTime is required",
    "beginTime must be formatted as YYYYMMDD",
    "pageNum must be a positive integer"
  ]
}
```

上游异常时，后端会返回对应错误：

```json
{
  "code": 502,
  "message": "upstream request failed",
  "detail": "..."
}
```

常见状态码：

| 状态码 | 说明 |
| --- | --- |
| `200` | 请求成功，响应内容来自数据中台 |
| `400` | 前端请求参数格式错误 |
| `500` | 后端数据中台配置缺失 |
| `502` | 数据中台请求失败或返回非 JSON |
| `504` | 数据中台请求超时 |

## 辅助接口

### 健康检查

```http
GET /api/v1/health
```

响应：

```json
{
  "status": "ok"
}
```

### 接口元数据

```http
GET /api/v1/endpoints
```

用于查看当前后端已注册的接口清单、必填字段和可选字段。

## 业务接口清单

| 序号 | 功能 | 方法 | 本系统接口 |
| --- | --- | --- | --- |
| 1 | 诉求类型数量 Top5 | `POST` | `/api/v1/sensitive-appeals/appeal-type-count/top5` |
| 2 | 重要/敏感/特殊用户数量统计 | `POST` | `/api/v1/sensitive-appeals/user-type-count` |
| 3 | 用户诉求信息查询 | `POST` | `/api/v1/sensitive-appeals/user-appeals` |
| 4 | 重要用户类型数量统计 | `POST` | `/api/v1/sensitive-appeals/key-users/type-count` |
| 5 | 重要用户列表 | `POST` | `/api/v1/sensitive-appeals/key-users` |
| 6 | 重要用户详情 | `POST` | `/api/v1/sensitive-appeals/key-users/detail` |
| 7 | 敏感用户类型数量统计 | `POST` | `/api/v1/sensitive-appeals/sensitive-users/type-count` |
| 8 | 敏感用户列表 | `POST` | `/api/v1/sensitive-appeals/sensitive-users` |
| 9 | 敏感用户详情 | `POST` | `/api/v1/sensitive-appeals/sensitive-users/detail` |
| 10 | 特殊用户类型数量统计 | `POST` | `/api/v1/sensitive-appeals/special-users/type-count` |
| 11 | 特殊用户列表 | `POST` | `/api/v1/sensitive-appeals/special-users` |
| 12 | 特殊用户详情 | `POST` | `/api/v1/sensitive-appeals/special-users/detail` |

---

## 1. 诉求类型数量 Top5

```http
POST /api/v1/sensitive-appeals/appeal-type-count/top5
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 是 | string | 开始日期，格式 `YYYYMMDD` |
| `endTime` | 是 | string | 结束日期，格式 `YYYYMMDD` |

### 请求示例

```json
{
  "beginTime": "20260701",
  "endTime": "20260701"
}
```

### 响应 data 字段

`data` 为数组，单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `cust_name` | 客户名称 |
| `cust_no` | 客户编号 |
| `calling_number` | 来电号码 |
| `appeal_cnt` | 诉求总次数 |
| `category1_cnt` ~ `category6_cnt` | 各诉求类型次数 |

---

## 2. 重要/敏感/特殊用户数量统计

```http
POST /api/v1/sensitive-appeals/user-type-count
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 是 | string | 开始日期，格式 `YYYYMMDD` |
| `endTime` | 是 | string | 结束日期，格式 `YYYYMMDD` |

### 请求示例

```json
{
  "beginTime": "20260701",
  "endTime": "20260701"
}
```

### 响应 data 字段

`data` 为数组，单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `key_cnt` | 重要用户数量 |
| `sensitive_cnt` | 敏感用户数量 |
| `spcl_cnt` | 特殊用户数量 |

---

## 3. 用户诉求信息查询

```http
POST /api/v1/sensitive-appeals/user-appeals
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 是 | string | 开始日期，格式 `YYYYMMDD` |
| `endTime` | 是 | string | 结束日期，格式 `YYYYMMDD` |
| `cust_no` | 否 | string | 客户编号 |
| `cust_name` | 否 | string | 客户名称 |

### 请求示例

```json
{
  "beginTime": "20260701",
  "endTime": "20260701",
  "cust_no": "1601175532147"
}
```

### 响应 data 字段

`data` 为数组，单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `cust_name` | 客户名称 |
| `cust_no` | 客户编号 |
| `calling_number` | 来电号码 |
| `addr` | 地址 |
| `acpt_time` | 受理时间 |
| `acpt_cont` | 诉求内容 |

---

## 4. 重要用户类型数量统计

```http
POST /api/v1/sensitive-appeals/key-users/type-count
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 是 | string | 开始日期，格式 `YYYYMMDD` |
| `endTime` | 是 | string | 结束日期，格式 `YYYYMMDD` |

### 请求示例

```json
{
  "beginTime": "20260701",
  "endTime": "20260701"
}
```

### 响应 data 字段

`data` 为数组，单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `medical_cnt` | 医疗卫生类数量 |
| `education_cnt` | 教育机构类数量 |
| `gov_cnt` | 党政机关类数量 |
| `comuni_cnt` | 信息通信类数量 |
| `water_cnt` | 城市供水类数量 |
| `heat_cnt` | 城市供热类数量 |
| `gas_cnt` | 城市燃气类数量 |
| `traffic_cnt` | 交通运输类数量 |

---

## 5. 重要用户列表

```http
POST /api/v1/sensitive-appeals/key-users
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 是 | string | 开始日期，格式 `YYYYMMDD` |
| `endTime` | 是 | string | 结束日期，格式 `YYYYMMDD` |
| `pageNum` | 是 | number/string | 当前页，正整数 |
| `pageSize` | 是 | number/string | 每页条数，正整数 |
| `cust_name` | 否 | string | 客户名称 |
| `impt_cust_type_name` | 否 | string | 重要用户类型名称 |
| `returnTotalNum` | 否 | boolean/string | 是否返回总数，常用值为 `true` 或 `"true"` |

### 请求示例

```json
{
  "returnTotalNum": "true",
  "pageNum": "1",
  "pageSize": "10",
  "beginTime": "20260701",
  "endTime": "20260701"
}
```

### 响应 data 字段

`data` 为分页对象：

| 字段 | 说明 |
| --- | --- |
| `totalNum` | 总条数 |
| `pageSize` | 每页条数 |
| `pageNum` | 当前页 |
| `rows` | 列表数据 |

`rows` 单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `cust_no` | 客户编号 |
| `cust_name` | 客户名称 |
| `calling_number` | 来电号码 |
| `impt_cust_type_name` | 重要用户类型名称 |

---

## 6. 重要用户详情

```http
POST /api/v1/sensitive-appeals/key-users/detail
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 是 | string | 开始日期，格式 `YYYYMMDD` |
| `endTime` | 是 | string | 结束日期，格式 `YYYYMMDD` |
| `cust_no` | 是 | string | 客户编号 |

### 请求示例

```json
{
  "beginTime": "20260701",
  "endTime": "20260701",
  "cust_no": "1601100878054"
}
```

### 响应 data 字段

`data` 为数组，单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `cust_no` | 客户编号 |
| `cust_name` | 客户名称 |
| `calling_number` | 来电号码 |
| `impt_cust_type_name` | 重要用户类型名称 |
| `acpt_time` | 受理时间 |
| `acpt_cont` | 诉求内容 |

---

## 7. 敏感用户类型数量统计

```http
POST /api/v1/sensitive-appeals/sensitive-users/type-count
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 否 | string | 开始日期，传入时格式必须为 `YYYYMMDD` |
| `endTime` | 否 | string | 结束日期，传入时格式必须为 `YYYYMMDD` |

### 请求示例

```json
{
  "beginTime": "20260701",
  "endTime": "20260701"
}
```

也可以不传日期：

```json
{}
```

### 响应 data 字段

`data` 为数组，单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `type1_cnt` | 敏感用户类型 1 数量 |
| `type2_cnt` | 敏感用户类型 2 数量 |
| `type3_cnt` | 敏感用户类型 3 数量 |
| `type4_cnt` | 敏感用户类型 4 数量 |

---

## 8. 敏感用户列表

```http
POST /api/v1/sensitive-appeals/sensitive-users
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 是 | string | 开始日期，格式 `YYYYMMDD` |
| `endTime` | 是 | string | 结束日期，格式 `YYYYMMDD` |
| `pageNum` | 是 | number/string | 当前页，正整数 |
| `pageSize` | 是 | number/string | 每页条数，正整数 |
| `cust_name` | 否 | string | 客户名称 |
| `sensitive_cust_type_name` | 否 | string | 敏感用户类型名称 |
| `returnTotalNum` | 否 | boolean/string | 是否返回总数，常用值为 `true` 或 `"true"` |

### 请求示例

```json
{
  "returnTotalNum": "true",
  "pageNum": "1",
  "pageSize": "10",
  "beginTime": "20260701",
  "endTime": "20260701"
}
```

### 响应 data 字段

`data` 为分页对象，结构同“重要用户列表”。

`rows` 单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `cust_no` | 客户编号 |
| `cust_name` | 客户名称 |
| `calling_number` | 来电号码 |
| `sensitive_cust_type_name` | 敏感用户类型名称 |

---

## 9. 敏感用户详情

```http
POST /api/v1/sensitive-appeals/sensitive-users/detail
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 是 | string | 开始日期，格式 `YYYYMMDD` |
| `endTime` | 是 | string | 结束日期，格式 `YYYYMMDD` |
| `cust_no` | 是 | string | 客户编号 |

### 请求示例

```json
{
  "beginTime": "20260701",
  "endTime": "20260701",
  "cust_no": "1601186720368"
}
```

### 响应 data 字段

`data` 为数组，单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `cust_no` | 客户编号 |
| `cust_name` | 客户名称 |
| `calling_number` | 来电号码 |
| `sensitive_cust_type_name` | 敏感用户类型名称 |
| `acpt_time` | 受理时间 |
| `acpt_cont` | 诉求内容 |

---

## 10. 特殊用户类型数量统计

```http
POST /api/v1/sensitive-appeals/special-users/type-count
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 否 | string | 开始日期，传入时格式必须为 `YYYYMMDD` |
| `endTime` | 否 | string | 结束日期，传入时格式必须为 `YYYYMMDD` |

### 请求示例

```json
{
  "beginTime": "20260701",
  "endTime": "20260701"
}
```

也可以不传日期：

```json
{}
```

### 响应 data 字段

`data` 为数组，单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `stype1_cnt` | 特殊用户类型 1 数量 |
| `stype2_cnt` | 特殊用户类型 2 数量 |
| `stype3_cnt` | 特殊用户类型 3 数量 |
| `stype4_cnt` | 特殊用户类型 4 数量 |
| `stype5_cnt` | 特殊用户类型 5 数量 |
| `stype6_cnt` | 特殊用户类型 6 数量 |
| `stype7_cnt` | 特殊用户类型 7 数量 |

---

## 11. 特殊用户列表

```http
POST /api/v1/sensitive-appeals/special-users
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 是 | string | 开始日期，格式 `YYYYMMDD` |
| `endTime` | 是 | string | 结束日期，格式 `YYYYMMDD` |
| `pageNum` | 是 | number/string | 当前页，正整数 |
| `pageSize` | 是 | number/string | 每页条数，正整数 |
| `cust_name` | 否 | string | 客户名称 |
| `spcl_cust_type_name` | 否 | string | 特殊用户类型名称 |
| `returnTotalNum` | 否 | boolean/string | 是否返回总数，常用值为 `true` 或 `"true"` |

### 请求示例

```json
{
  "returnTotalNum": "true",
  "pageNum": "1",
  "pageSize": "10",
  "beginTime": "20260701",
  "endTime": "20260701"
}
```

### 响应 data 字段

`data` 为分页对象，结构同“重要用户列表”。

`rows` 单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `cust_no` | 客户编号 |
| `cust_name` | 客户名称 |
| `calling_number` | 来电号码 |
| `spcl_cust_type_name` | 特殊用户类型名称 |

---

## 12. 特殊用户详情

```http
POST /api/v1/sensitive-appeals/special-users/detail
```

### 请求参数

| 参数 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `beginTime` | 是 | string | 开始日期，格式 `YYYYMMDD` |
| `endTime` | 是 | string | 结束日期，格式 `YYYYMMDD` |
| `cust_no` | 是 | string | 客户编号 |

### 请求示例

```json
{
  "beginTime": "20260701",
  "endTime": "20260701",
  "cust_no": "1603104540029"
}
```

### 响应 data 字段

`data` 为数组，单条记录常见字段：

| 字段 | 说明 |
| --- | --- |
| `cust_no` | 客户编号 |
| `cust_name` | 客户名称 |
| `calling_number` | 来电号码 |
| `spcl_cust_type_name` | 特殊用户类型名称 |
| `acpt_time` | 受理时间 |
| `acpt_cont` | 诉求内容 |

## 前端接入建议

- 请求时统一封装 `POST JSON`，不要在前端处理数据中台鉴权。
- 列表页默认传 `returnTotalNum: "true"`，便于分页组件展示总条数。
- 查询日期建议前端统一格式化为 `YYYYMMDD` 后再提交。
- `data` 结构由数据中台决定，前端应兼容 `data` 为空数组、`rows` 为空数组、类型名称字段为 `null` 的情况。
- 如果接口返回 `400`，优先检查必填字段、日期格式、分页参数。
