# 停电用户模块前端接口文档

本文档面向前端对接本系统接口。前端只需要请求本系统 `/api/v1/outage-users/...` 地址，不需要直接请求数据中台 `/cst/...` 地址，也不需要传 `appcode`。

## 通用说明

- Base URL：以后端部署地址为准，例如 `http://localhost:5000`
- 接口前缀：`/api/v1`
- 请求方法：除健康检查和接口清单外，本模块均为 `POST`
- 请求体：`Content-Type: application/json`
- 日期格式：`YYYYMMDD`，例如 `20260701`
- 分页字段：`pageNum`、`pageSize` 必须为正整数，可传数字或数字字符串
- 区域筛选：通常传 `cityId` 查询全市，传 `countyId` 查询指定区县
- 返回数据：业务接口基本透传数据中台返回结构

通用成功响应结构：

```json
{
  "data": [],
  "errCode": 0,
  "requestId": "string",
  "errMsg": "success",
  "apiLog": null
}
```

分页接口的 `data` 通常为：

```json
{
  "data": {
    "totalNum": 100,
    "pageSize": 10,
    "pageNum": 1,
    "rows": []
  },
  "errCode": 0,
  "requestId": "string",
  "errMsg": "success",
  "apiLog": null
}
```

本系统参数校验失败时返回 HTTP `400`：

```json
{
  "error": {
    "code": 400,
    "message": "invalid request payload",
    "errors": ["beginTime is required"]
  }
}
```

## 接口总览

| 序号 | 页面/用途 | 本系统接口 |
| --- | --- | --- |
| 1 | 获取区县列表 | `POST /api/v1/outage-users/counties` |
| 2 | 区县用户数统计 | `POST /api/v1/outage-users/county-user-count` |
| 3 | 时间趋势 | `POST /api/v1/outage-users/time-trend` |
| 4 | 空间分布 | `POST /api/v1/outage-users/spatial-distribution` |
| 5 | 区域警示灯 | `POST /api/v1/outage-users/region-warning-lights` |
| 6 | 电力故障定位 | `POST /api/v1/outage-users/fault-location` |
| 7 | 停电范围评估 | `POST /api/v1/outage-users/outage-scope-assessment` |
| 8 | 用户标签识别二级页饼图 | `POST /api/v1/outage-users/user-tags/chart` |
| 9 | 用户标签识别二级页列表 | `POST /api/v1/outage-users/user-tags` |
| 10 | 时间趋势二级页饼图 | `POST /api/v1/outage-users/time-trend/chart` |
| 11 | 时间趋势二级页列表 | `POST /api/v1/outage-users/time-trend/users` |
| 12 | 空间分布二级页设备影响摘要 | `POST /api/v1/outage-users/spatial-distribution/equipment-summary` |
| 13 | 空间分布二级页设备列表 | `POST /api/v1/outage-users/spatial-distribution/equipment` |
| 14 | 区域警示灯二级页 | `POST /api/v1/outage-users/region-warning-lights/detail` |
| 15 | 电力故障定位二级页事件类型 | `POST /api/v1/outage-users/fault-location/event-types` |
| 16 | 电力故障定位二级页线路列表 | `POST /api/v1/outage-users/fault-location/lines` |
| 17 | 电力故障定位二级页线路详情 | `POST /api/v1/outage-users/fault-location/lines/detail` |
| 18 | 停电范围评估二级页 | `POST /api/v1/outage-users/outage-scope-assessment/detail` |

## 1. 获取区县列表

`POST /api/v1/outage-users/counties`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `cityId` | 否 | 地市 ID；不传时由上游决定返回范围 |

请求示例：

```json
{
  "cityId": "16401"
}
```

返回字段：

| 字段 | 说明 |
| --- | --- |
| `cityId` | 地市 ID |
| `countyId` | 区县 ID |
| `countyName` | 区县/供电单位名称 |

## 2. 区县用户数统计

`POST /api/v1/outage-users/county-user-count`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 是 | 开始日期，`YYYYMMDD` |
| `endTime` | 是 | 结束日期，`YYYYMMDD` |
| `cityId` | 否 | 地市 ID，查询全市/全部区县时传 |
| `countyId` | 否 | 区县 ID，查询指定区县时传 |

请求示例：

```json
{
  "beginTime": "20260701",
  "endTime": "20260702",
  "cityId": "16401"
}
```

返回字段：

| 字段 | 说明 |
| --- | --- |
| `keyUsers` | 重点用户数 |
| `sensitiveUsers` | 敏感用户数 |
| `totalUsers` | 总用户数 |
| `countyName` | 区县/统计范围名称 |

## 3. 时间趋势

`POST /api/v1/outage-users/time-trend`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 是 | 开始日期，`YYYYMMDD` |
| `endTime` | 是 | 结束日期，`YYYYMMDD` |
| `cityId` | 否 | 地市 ID |
| `countyId` | 否 | 区县 ID |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `timePoint` | 日期点，`YYYYMMDD` |
| `keyUsers` | 重点用户数 |
| `sensitiveUsers` | 敏感用户数 |

## 4. 空间分布

`POST /api/v1/outage-users/spatial-distribution`

请求参数同“时间趋势”。

返回字段：

| 字段 | 说明 |
| --- | --- |
| `name` | 区县/供电单位名称 |
| `keyUsers` | 重点用户数 |
| `sensitiveUsers` | 敏感用户数 |

## 5. 区域警示灯

`POST /api/v1/outage-users/region-warning-lights`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 否 | 开始日期，`YYYYMMDD` |
| `endTime` | 否 | 结束日期，`YYYYMMDD` |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `county_id` | 区县 ID |
| `county_name` | 区县/供电单位名称 |
| `hasOutage` | 是否存在停电，字符串值如 `"true"`/`"false"` |

## 6. 电力故障定位

`POST /api/v1/outage-users/fault-location`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 否 | 开始日期，`YYYYMMDD` |
| `endTime` | 否 | 结束日期，`YYYYMMDD` |
| `city_id` | 否 | 地市 ID；当前后端也兼容 `city` |
| `county_id` | 否 | 区县 ID；当前后端也兼容 `countyId` |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `total` | 总线路/事件数量 |
| `safe` | 安全数量 |
| `warning` | 预警数量 |
| `danger` | 危险数量 |

## 7. 停电范围评估

`POST /api/v1/outage-users/outage-scope-assessment`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 是 | 开始日期，`YYYYMMDD` |
| `endTime` | 是 | 结束日期，`YYYYMMDD` |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `lineCnt` | 线路数量 |
| `subsCnt` | 变电站/台区相关数量 |

## 8. 用户标签识别二级页饼图

`POST /api/v1/outage-users/user-tags/chart`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 是 | 开始日期，`YYYYMMDD` |
| `endTime` | 是 | 结束日期，`YYYYMMDD` |
| `cityId` | 否 | 地市 ID；当前后端也兼容 `city` |
| `countyId` | 否 | 区县 ID；当前后端也兼容 `county` |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `tradeName` | 行业/标签名称 |
| `keyUsersCnt` | 重点用户数量 |
| `sensitiveUsersCnt` | 敏感用户数量 |

注意：原始文档中该接口部分场景的 `data` 可能是 JSON 字符串，部分场景是数组。前端建议兼容：如果 `typeof data === "string"`，先 `JSON.parse(data)` 后再渲染。

## 9. 用户标签识别二级页列表

`POST /api/v1/outage-users/user-tags`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 是 | 开始日期，`YYYYMMDD` |
| `endTime` | 是 | 结束日期，`YYYYMMDD` |
| `pageNum` | 是 | 页码，正整数 |
| `pageSize` | 是 | 每页条数，正整数 |
| `returnTotalNum` | 否 | 是否返回总数，常用 `"true"` |
| `cityId` | 否 | 地市 ID；当前后端也兼容 `city` |
| `countyId` | 否 | 区县 ID；当前后端也兼容 `county` |
| `custName` | 否 | 用户名称搜索 |
| `isKey` | 否 | 是否重点用户，`"1"` 是，`"0"` 否 |
| `isSen` | 否 | 是否敏感用户，`"1"` 是，`"0"` 否 |

请求示例：

```json
{
  "beginTime": "20260701",
  "endTime": "20260701",
  "pageNum": "1",
  "pageSize": "10",
  "cityId": "16401",
  "returnTotalNum": "true"
}
```

列表行字段：

| 字段 | 说明 |
| --- | --- |
| `consNo` | 用户编号 |
| `consName` | 用户名称 |
| `countyName` | 区县/供电单位名称 |
| `tradeName` | 行业名称 |
| `custTypeName` | 用户类型名称 |
| `isKey` | 是否重点用户，`"1"` 是，`"0"` 否 |
| `isSen` | 是否敏感用户，`"1"` 是，`"0"` 否 |

## 10. 时间趋势二级页饼图

`POST /api/v1/outage-users/time-trend/chart`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 是 | 开始日期，`YYYYMMDD` |
| `endTime` | 是 | 结束日期，`YYYYMMDD` |
| `cityId` | 否 | 地市 ID |
| `countyId` | 否 | 区县 ID |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `outageKeyOne` | 停电 1 次的重点用户数 |
| `outageKeyTwo` | 停电 2 次的重点用户数 |
| `outageKeyMore` | 停电 3 次及以上的重点用户数 |
| `keyTotal` | 重点用户总数 |
| `outageSenOne` | 停电 1 次的敏感用户数 |
| `outageSenTwo` | 停电 2 次的敏感用户数 |
| `outageSenMore` | 停电 3 次及以上的敏感用户数 |
| `senTotal` | 敏感用户总数 |

## 11. 时间趋势二级页列表

`POST /api/v1/outage-users/time-trend/users`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 是 | 开始日期，`YYYYMMDD` |
| `endTime` | 是 | 结束日期，`YYYYMMDD` |
| `pageNum` | 是 | 页码，正整数 |
| `pageSize` | 是 | 每页条数，正整数 |
| `returnTotalNum` | 否 | 是否返回总数，常用 `"true"` |
| `cityId` | 否 | 地市 ID |
| `countyId` | 否 | 区县 ID |
| `custName` | 否 | 用户名称搜索 |

列表行字段：

| 字段 | 说明 |
| --- | --- |
| `consNo` | 用户编号 |
| `consName` | 用户名称 |
| `countyName` | 区县/供电单位名称 |
| `intervalOne` | 时间区间一内停电次数 |
| `intervalTwo` | 时间区间二内停电次数 |

## 12. 空间分布二级页设备影响摘要

`POST /api/v1/outage-users/spatial-distribution/equipment-summary`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 是 | 开始日期，`YYYYMMDD` |
| `endTime` | 是 | 结束日期，`YYYYMMDD` |
| `cityId` | 否 | 地市 ID |
| `countyId` | 否 | 区县 ID |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `equipmentCount` | 受影响设备数量 |
| `keyUsersCnt` | 受影响重点用户数 |
| `sensitiveUsersCnt` | 受影响敏感用户数 |

## 13. 空间分布二级页设备列表

`POST /api/v1/outage-users/spatial-distribution/equipment`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 是 | 开始日期，`YYYYMMDD` |
| `endTime` | 是 | 结束日期，`YYYYMMDD` |
| `pageNum` | 是 | 页码，正整数 |
| `pageSize` | 是 | 每页条数，正整数 |
| `returnTotalNum` | 否 | 是否返回总数，常用 `"true"` |
| `cityId` | 否 | 地市 ID；当前后端也兼容 `city` |
| `countyId` | 否 | 区县 ID；当前后端也兼容 `county` |

列表行字段：

| 字段 | 说明 |
| --- | --- |
| `equipmentId` | 设备 ID |
| `equipmentName` | 设备名称 |
| `keyUsersCnt` | 重点用户数 |
| `senUsersCnt` | 敏感用户数 |
| `allUsersCnt` | 总用户数 |

## 14. 区域警示灯二级页

`POST /api/v1/outage-users/region-warning-lights/detail`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `county_id` | 是 | 区县 ID |
| `beginTime` | 否 | 开始日期，`YYYYMMDD` |
| `endTime` | 否 | 结束日期，`YYYYMMDD` |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `county_name` | 区县/供电单位名称 |
| `key_users_cnt` | 重点用户数 |
| `sen_users_cnt` | 敏感用户数 |
| `all_users_cnt` | 总用户数 |

## 15. 电力故障定位二级页事件类型

`POST /api/v1/outage-users/fault-location/event-types`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `beginTime` | 否 | 开始日期，`YYYYMMDD` |
| `endTime` | 否 | 结束日期，`YYYYMMDD` |

返回字段：

| 字段 | 说明 |
| --- | --- |
| `real_time_event_type_id` | 事件类型 ID |
| `real_time_event_type_name` | 事件类型名称 |
| `real_time_event_num` | 事件数量 |

## 16. 电力故障定位二级页线路列表

`POST /api/v1/outage-users/fault-location/lines`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `pageNum` | 是 | 页码，正整数 |
| `pageSize` | 是 | 每页条数，正整数 |
| `beginTime` | 否 | 开始日期，`YYYYMMDD` |
| `endTime` | 否 | 结束日期，`YYYYMMDD` |
| `returnTotalNum` | 否 | 是否返回总数，常用 `"true"` |
| `line_id` | 否 | 线路编号 |
| `outage_type_name` | 否 | 停电类型名称 |

列表行字段：

| 字段 | 说明 |
| --- | --- |
| `line_id` | 线路编号 |
| `line_name` | 线路名称 |
| `subs_name` | 变电站名称 |
| `outage_type_name` | 停电类型名称 |

## 17. 电力故障定位二级页线路详情

`POST /api/v1/outage-users/fault-location/lines/detail`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `line_id` | 是 | 线路编号 |
| `beginTime` | 否 | 开始日期，`YYYYMMDD` |
| `endTime` | 否 | 结束日期，`YYYYMMDD` |

请求示例：

```json
{
  "beginTime": "20260701",
  "endTime": "20260701",
  "line_id": "88AB24BF-84D5-4F75-B4B1-DDD88DA48F3B-29790"
}
```

返回字段：

| 字段 | 说明 |
| --- | --- |
| `line_id` | 线路编号 |
| `line_name` | 线路名称 |
| `subs_name` | 变电站名称 |
| `outage_type_name` | 停电类型名称 |
| `outage_time` | 停电时间 |
| `trans_time` | 送电/转供时间 |
| `outage_reason` | 停电原因 |

## 18. 停电范围评估二级页

`POST /api/v1/outage-users/outage-scope-assessment/detail`

请求参数：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `pageNum` | 是 | 页码，正整数 |
| `pageSize` | 是 | 每页条数，正整数 |
| `beginTime` | 否 | 开始日期，`YYYYMMDD` |
| `endTime` | 否 | 结束日期，`YYYYMMDD` |
| `returnTotalNum` | 否 | 是否返回总数，常用 `"true"` |

列表行字段：

| 字段 | 说明 |
| --- | --- |
| `line_id` | 线路编号 |
| `outage_scope` | 停电影响范围描述 |

## 调用示例

```js
async function fetchOutageUserTags() {
  const res = await fetch("/api/v1/outage-users/user-tags", {
    method: "POST",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      beginTime: "20260701",
      endTime: "20260701",
      pageNum: 1,
      pageSize: 10,
      cityId: "16401",
      returnTotalNum: "true"
    })
  });

  const body = await res.json();
  if (!res.ok) {
    throw new Error(body?.error?.message || "接口请求失败");
  }

  return body.data;
}
```

## 前端对接注意事项

1. 字段名大小写和下划线需要严格按文档传递，例如 `countyId` 与 `county_id` 是不同字段。
2. 后端只做轻量参数校验，上游业务错误会按上游响应透传。
3. 原始接口文档中的部分中文示例存在编码乱码，本文档只整理字段含义；真实名称以接口实际返回为准。
4. 可通过 `GET /api/v1/endpoints` 查看当前后端实际注册的接口清单。
